{
  "name": "Dotfiles Devcontainer (Remote Host)",
  "extends": "./devcontainer.json",
  "remoteEnv": {
    "OP_SERVICE_ACCOUNT_TOKEN": "${localEnv:OP_SERVICE_ACCOUNT_TOKEN}"
  },
  "secrets": {
    "OP_SERVICE_ACCOUNT_TOKEN": {
      "description": "1Password service account for chezmoi age + Coolify token"
    },
    "TAILSCALE_AUTHKEY": {
      "description": "Reusable or ephemeral Tailscale auth key for this devcontainer"
    }
  }
}
